#include"main.h"
/**
 * mul - function to multiplies two integers
 * @a: first variable to be use for multiplication
 * @b: second variable to be use for multiplication
 * Return: always return 0
*/

int mul(int a, int b)
{
	return (a * b);
}
